package com.resquircle

/**
 * RN version-specific compat layer for React Native >= 0.83.
 * Add 0.83+ implementations here when needed (e.g. BackgroundImageDrawable, new drawable pipeline).
 */
internal object ResquircleCompat {
  const val RN_MINOR = 83
}
